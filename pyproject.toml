[project]
name = "resipi"
version = "1.0.0"
description = "A conversation-to-workflow compiler for micro-businesses"
requires-python = ">=3.12"
dependencies = []

[tool.vercel]
# Pinned explicitly: the package directory is named `app/`, which would
# otherwise collide with Vercel's entrypoint auto-detection.
entrypoint = "index:app"
